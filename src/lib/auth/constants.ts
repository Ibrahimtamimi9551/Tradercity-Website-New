/** DEV MOCK ONLY — replace with NestJS JWT/session during backend auth integration. */

export const AUTH_IS_DEV_MOCK = true as const;

export const MOCK_SESSION_STORAGE_KEY = "tradercity.dev.mockSession.v1";

export const DEFAULT_POST_AUTH_PATH = "/dashboard/free";

export const MOCK_AUTH_LATENCY_MS = 500;
