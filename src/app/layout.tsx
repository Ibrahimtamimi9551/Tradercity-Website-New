import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "TraderCity — Crypto Intelligence Ecosystem",
  description:
    "One ecosystem. Multiple experts. Professional research, learning frameworks, analyst insights, and community for serious crypto traders.",
  openGraph: {
    title: "TraderCity — Crypto Intelligence Ecosystem",
    description:
      "One ecosystem. Multiple experts. Professional research, learning frameworks, and community for serious traders.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
